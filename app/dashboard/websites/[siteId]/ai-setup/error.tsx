"use client";

import { Card } from "@/components/ui/card";

export default function ErrorPage() {
  return <Card className="p-6 text-sm text-muted">AI setup could not load. Refresh the page or continue with manual editing.</Card>;
}
