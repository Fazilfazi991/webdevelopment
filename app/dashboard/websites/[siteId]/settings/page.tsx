import { ChevronRight, Code2, Palette, Search, Settings2 } from "lucide-react";
import Link from "next/link";
export default function WebsiteSettingsPage({ params }: { params: { siteId: string } }) { const base = `/dashboard/websites/${params.siteId}`; return <div className="mx-auto max-w-4xl"><div><p className="text-xs font-bold uppercase tracking-widest text-brand-700">Website</p><h1 className="mt-2 text-3xl font-bold text-ink">Settings</h1></div><div className="mt-6 overflow-hidden rounded-xl bg-white shadow-[0_12px_36px_rgba(24,33,31,0.1)]">{[{ label: "Branding", body: "Logo, favicon, social image, and display options", href: `${base}/settings/branding`, icon: Palette }, { label: "Website Address", body: "Studio OS address and custom domains", href: `${base}/domains`, icon: Settings2 }, { label: "Google Search Setup", body: "Ownership verification for Search Console", href: `${base}/seo`, icon: Search }, { label: "Advanced", body: "Permission-gated developer code and recovery tools", href: `${base}/settings/advanced`, icon: Code2 }].map((item) => <Link key={item.label} href={item.href} className="flex min-h-20 items-center gap-4 border-b border-line px-4 py-3 last:border-b-0 hover:bg-canvas"><span className="flex size-11 items-center justify-center rounded-lg bg-brand-50 text-brand-700"><item.icon size={20} /></span><span className="min-w-0 flex-1"><span className="block font-bold text-ink">{item.label}</span><span className="mt-1 block text-sm text-muted">{item.body}</span></span><ChevronRight size={18} className="text-muted" /></Link>)}</div></div>; }
