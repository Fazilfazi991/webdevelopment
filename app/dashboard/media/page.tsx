import { EmptyState } from "@/components/ui/card";

export default function MediaPage() {
  return (
    <EmptyState
      title="Media library is planned"
      description="Uploads, image metadata, reusable assets, and approved placeholders are prepared in the database and will be connected in a later phase."
    />
  );
}
