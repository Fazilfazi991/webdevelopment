import { EmptyState } from "@/components/ui/card";

export default function ClientWebsiteNotFound() {
  return <EmptyState title="Website not available" description="You do not have access to this website, or the invitation has not been accepted." />;
}
