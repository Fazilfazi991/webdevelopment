"use client";

import { Card } from "@/components/ui/card";

export default function ErrorPage() {
  return <Card className="p-6 text-sm text-muted">AI suggestions could not load. Existing content was not changed.</Card>;
}
